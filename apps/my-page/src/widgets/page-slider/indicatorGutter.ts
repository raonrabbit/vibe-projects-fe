/** `md`(768px) 미만: 점만 / 이상: 라벨 + 점 */
export const PAGE_INDICATOR_LABEL_CLASS = "hidden md:inline-flex";

/** 본문이 `RightIndicator`와 겹치지 않도록 확보하는 좌우 패딩 */
export const PAGE_INDICATOR_GUTTER_CLASS = "px-6 md:px-[7.5rem]";
